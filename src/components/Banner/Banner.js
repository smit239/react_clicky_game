import React from 'react';
import './Banner.css';

const Banner = () => (
  
  <div className="banner text-center d-flex align-items-center justify-content-center">
    <p className="m-0">Click on an image below to earn points, but don't click on the same image more than once!</p>
  </div>
);

export default Banner;
